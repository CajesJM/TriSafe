import 'package:flutter/material.dart';

import '../shared/app_layout_styles.dart';

abstract final class PassengerProfileStyles {
  static const EdgeInsets screenPadding = AppLayoutStyles.mobileScreenPadding;
  static const EdgeInsets profileCardPadding = EdgeInsets.all(20);
  static const double profileRadius = AppLayoutStyles.panelRadius;
}
